name: 🤗 Deploy no Hugging Face
on:
  workflow_run:
    workflows:  ["🚀 Production Pipeline"]
    types:
      - completed
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-depth: 0
          
      - name: Setup LFS and Push
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git lfs install
          git lfs fetch --all
          git push --force https://emn-f:$HF_TOKEN@huggingface.co/spaces/emn-f/vox-ai master:main
